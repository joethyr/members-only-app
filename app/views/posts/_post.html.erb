<div id="<%= dom_id post %>">
  <p>
    <strong>Title:</strong>
    <%= post.title %>
  </p>

  <p>
    <strong>Body:</strong>
    <%= post.body %>
  </p>

  <% if user_signed_in? %>
    <p>
      <strong>Author:</strong>
      <%= User.find(post.user_id).username %>
    </p>
  <% end %>
</div>
